4. Advanced Intro to python

```python
#!/bin/python3
import sys #Import sys, system functions and parameters
#from datetime import datetime #Only imports datetime part of datetime
from datetime import datetime as dt #Imports datetime as dt alias

print(dt.now())

my_name = "Health"
print(my_name[0])
print(my_name[-1])

sentence = "This is a sentence."
print(sentence[:4])

print(sentence.split()) #Split words at spaces

sentence_split = sentence.split()
sentence_join = ' '.join(sentence_split)
print(sentence_join)

print("He said, \"give me all your money\"")

too_much_space = "                  hello                        "
print(too_much_space.strip()) #Remove spaces at beginning and end

print("a" in "Apple") #False, case sensitive

letter = "A"
word = "Apple"
print(letter.lower() in word.lower()) #True, since case insensitive

movie = "The Matrix"
print("My favorite movie is " + movie + ".")
#OR
print("My favorite movie is {}.".format(movie)) #Replace value in {}

#Dictionaries - key/value pairs {}
drinks = {"Water": 1, "Juice": 5, "Soda": 4}
print(drinks)

employees = {"Finance": ["Bob", "Linda", "Tina"], "IT": ["Gene", "Louise", "Toody"], "HR": ["Jimmy", "Mort"]}
print(employees)

employees['Legal'] = ['Mr. Frond'] #Add new key:value pair

print(employees)

employees.update({"Sales": ["Andie", "Ollie"]})

print(employees)

drinks['White Russian'] = 8
print(drinks)

print(drinks.get("White Russian"))
```